{
  "name": "@repo/template-api-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rimraf lib/ module/",
    "build:lib": "tsc",
    "build:module": "tsc -p tsconfig.module.json",
    "build": "run-s clean build:*"
  },
  "dependencies": {
    "@repo/ui": "workspace:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "15.0.0-rc.0"
  }
}
